What is JavaScript ?
=> JavaScript is a highly absracted pragramming language

absracted means hiding the implementation details , showing
only the functionality to the users.



***Interview questions

1. Tell me something about JS engine v8 internal mechanism.
2. What is event loop in JavaScript ? *****
3. If JavaScript is single threaded, how does it handle asynchronous call?
4. Difference between setTimeOut and SetInterval
5. Can you explain the difference between an async/await and promise?
6. What's the error handling strategy for promises that were rejected while awaiting?
7. Can you explain what the .then() method does?